# مخطط الكينونة والعلاقة (Chen ERD) المنظم للطباعة والإدراج في Word

هذا الملف يعيد **تنظيم** نفس مخطط Chen ERD الموجود في الملف الأصلي، لكن بطريقة أكثر قابلية للقراءة والإدراج داخل مستند Word.

- لا يوجد أي حذف لأي كيان أو علاقة أو خاصية.
- تم فقط **إعادة توزيع المخطط** إلى:
  1. لوحة رئيسية للعلاقات بين الكيانات.
  2. لوحات تفصيلية للخصائص حسب المجال الوظيفي.
- هذا الأسلوب يعطي شكلاً أكاديمياً أنظف ويقلل المساحة البصرية المهدورة.

## دليل الرموز

- **الكيان القوي:** مستطيل أزرق.
- **الكيان الضعيف:** مستطيل أحمر بإطار سميك.
- **العلاقة العادية:** معين ذهبي.
- **العلاقة المُعرِّفة للكيان الضعيف:** معين بني داكن بإطار سميك.
- **الخاصية العادية:** شكل بيضاوي فاتح.
- **الخاصية متعددة القيم:** بيضاوي بإطار سميك.
- **الخاصية المشتقة:** بيضاوي بخط متقطع.
- **الخاصية المركبة:** خاصية رئيسية تتفرع إلى خصائص فرعية.

---

## 1) اللوحة الرئيسية: خريطة العلاقات بين الكيانات

```mermaid
flowchart LR
    classDef entity fill:#2E86C1,stroke:#1B4F72,stroke-width:2px,color:#fff,font-weight:bold
    classDef weak_entity fill:#C0392B,stroke:#7B241C,stroke-width:4px,color:#fff,font-weight:bold
    classDef relation fill:#D68910,stroke:#9C640C,stroke-width:2px,color:#fff,font-weight:bold
    classDef weak_relation fill:#935116,stroke:#6E2C00,stroke-width:4px,color:#fff,font-weight:bold

    subgraph G1["إدارة العملاء والتأمين"]
        direction LR
        E_CLIENT["العميل"]:::entity
        R_1{"يقدم"}:::relation
        E_REQ["طلب التأمين"]:::entity
        R_9{"يولد"}:::relation
        E_POL["وثيقة التأمين"]:::entity
        R_12{"يغطي"}:::relation
        E_CLAIM["المطالبة"]:::entity
        E_RDOC["مرفق الطلب"]:::weak_entity
    end

    subgraph G2["هيكل المنتجات التأمينية"]
        direction TB
        E_CAT["مجال التأمين"]:::entity
        R_6{"يضم"}:::relation
        E_TYPE["نوع التأمين"]:::entity
        E_COVER["التغطية التأمينية"]:::entity
        E_PRICE["قاعدة التسعير"]:::entity
        E_DOCREQ["متطلب مستند"]:::entity
    end

    subgraph G3["المالية والتحصيل"]
        direction TB
        E_INST["القسط المستحق"]:::weak_entity
        R_11{"يسوى بـ"}:::relation
        E_PAY["عملية السداد"]:::entity
    end

    subgraph G4["المطالبات والتتبع"]
        direction TB
        E_CDOC["مرفق المطالبة"]:::weak_entity
        E_CSTAT["سجل حالة المطالبة"]:::weak_entity
        E_OBJ["اعتراض المطالبة"]:::weak_entity
    end

    subgraph G5["الإدارة والتشغيل"]
        direction TB
        E_ROLE["الدور"]:::entity
        E_PERM["صلاحية النظام"]:::entity
        E_USER["حساب المستخدم"]:::entity
        E_NOTIF["الإشعار"]:::entity
        E_AUDIT["سجل التدقيق والعمليات"]:::entity
    end

    E_CLIENT -- "1" --- R_1
    R_1 -- "M" --- E_REQ

    E_CLIENT -- "1" --- R_30{"يمتلك حساب"}:::relation
    R_30 -- "1" --- E_USER

    E_CLIENT -- "1" --- R_3{"ينشئ"}:::relation
    R_3 -- "M" --- E_CLAIM

    E_CLIENT -- "1" --- R_4{"يسدد"}:::relation
    R_4 -- "M" --- E_PAY

    E_CLIENT -- "1" --- R_5{"يستلم"}:::relation
    R_5 -- "M" --- E_NOTIF

    E_CAT -- "1" --- R_6
    R_6 -- "M" --- E_TYPE

    E_TYPE -- "1" --- R_7{"يطلب لـ"}:::relation
    R_7 -- "M" --- E_REQ

    E_TYPE -- "1" --- R_20{"يحدد"}:::relation
    R_20 -- "M" --- E_COVER

    E_TYPE -- "1" --- R_21{"تسعر بواسطة"}:::relation
    R_21 -- "M" --- E_PRICE

    E_TYPE -- "1" --- R_22{"يتطلب"}:::relation
    R_22 -- "M" --- E_DOCREQ

    E_PRICE -- "1" --- R_26{"تطبق على"}:::relation
    R_26 -- "M" --- E_REQ

    E_TYPE -- "1" --- R_8{"يصدر كـ"}:::relation
    R_8 -- "M" --- E_POL

    E_REQ -- "1" --- R_31{"مرفق بـ"}:::weak_relation
    R_31 -- "M" --- E_RDOC

    E_REQ -- "1" --- R_9
    R_9 -- "1" --- E_POL

    E_POL -- "M" --- R_28{"تتضمن"}:::relation
    R_28 -- "N" --- E_COVER

    %% ملاحظة: مشاركة الوثيقة في العلاقة جزئية (احتمال الرفض)، لكن كاردينالية العلاقة تظل 1:1
    E_USER -- "1" --- R_19{"يراجع/يعتمد"}:::relation
    R_19 -- "M" --- E_REQ

    E_POL -- "1" --- R_10{"يحتوي"}:::weak_relation
    R_10 -- "M" --- E_INST

    E_POL -- "1" --- R_23{"يرفق بـ"}:::weak_relation
    R_23 -- "M" --- E_PDOC["مرفق الوثيقة"]:::weak_entity

    E_POL -- "1" --- R_27{"يُعدّل بـ"}:::weak_relation
    R_27 -- "M" --- E_ENDORS["ملحق الوثيقة"]:::weak_entity

    E_INST -- "1" --- R_11
    R_11 -- "M" --- E_PAY

    E_POL -- "1" --- R_12
    R_12 -- "M" --- E_CLAIM

    E_CLAIM -- "1" --- R_13{"يشمل"}:::weak_relation
    R_13 -- "M" --- E_CDOC

    E_CLAIM -- "1" --- R_14{"يتتبع بـ"}:::weak_relation
    R_14 -- "M" --- E_CSTAT

    E_CLAIM -- "1" --- R_24{"يعترض على"}:::weak_relation
    R_24 -- "M" --- E_OBJ

    E_ROLE -- "1" --- R_15{"يسند إلى"}:::relation
    R_15 -- "M" --- E_USER

    E_ROLE -- "M" --- R_29{"تمتلك"}:::relation
    R_29 -- "N" --- E_PERM

    E_USER -- "1" --- R_16{"يسجل"}:::relation
    R_16 -- "M" --- E_AUDIT

    E_USER -- "1" --- R_17{"يحدث"}:::relation
    R_17 -- "M" --- E_CSTAT

    E_USER -- "1" --- R_25{"يعالج"}:::relation
    R_25 -- "M" --- E_OBJ

    E_USER -- "1" --- R_18{"يرسل له"}:::relation
    R_18 -- "M" --- E_NOTIF
```

---

## 2) لوحة الخصائص: العميل والحسابات الإدارية

```mermaid
flowchart LR
    classDef entity fill:#2E86C1,stroke:#1B4F72,stroke-width:2px,color:#fff,font-weight:bold
    classDef attribute fill:#F8F9F9,stroke:#AAB7B8,stroke-width:1.5px,color:#222
    classDef multi_attribute fill:#F8F9F9,stroke:#424949,stroke-width:3px,color:#222

    subgraph C1["العميل"]
        direction TB
        E_CLIENT["العميل"]:::entity
        AL1(["معرف العميل (PK)"]):::attribute
        AL2(["الاسم الكامل"]):::attribute
        AL2A(["الاسم الأول"]):::attribute
        AL2B(["اسم العائلة"]):::attribute
        AL3(["الهوية"]):::attribute
        AL4(["الهاتف"]):::multi_attribute
        AL5(["البريد"]):::attribute
        AL6(["العنوان"]):::attribute
        AL6A(["المدينة"]):::attribute
        AL6B(["الشارع"]):::attribute
        AL7(["الحالة"]):::attribute
    end

    subgraph C2["حساب المستخدم"]
        direction TB
        E_USER["حساب المستخدم"]:::entity
        AU1(["معرف المستخدم - PK"]):::attribute
        AU2(["اسم المستخدم"]):::attribute
        AU3(["كلمة المرور"]):::attribute
        AU4(["الاسم"]):::attribute
        AU5(["الحالة"]):::attribute
    end

    subgraph C3["الدور والصلاحية"]
        direction TB
        E_ROLE["الدور"]:::entity
        AR1(["معرف الدور - PK"]):::attribute
        AR2(["اسم الدور"]):::attribute
        AR3(["الوصف"]):::attribute

        E_PERM["صلاحية النظام"]:::entity
        PM1(["معرف الصلاحية - PK"]):::attribute
        PM2(["اسم الصلاحية"]):::attribute
    end

    subgraph C4["الإشعار"]
        direction TB
        E_NOTIF["الإشعار"]:::entity
        NO1(["معرف الإشعار - PK"]):::attribute
        NO2(["النوع"]):::attribute
        NO3(["القناة"]):::attribute
        NO4(["النص"]):::attribute
        NO5(["الحالة"]):::attribute
        NO6(["تاريخ الإرسال"]):::attribute
    end

    subgraph C5["سجل التدقيق والعمليات"]
        direction TB
        E_AUDIT["سجل التدقيق والعمليات"]:::entity
        AU_1(["معرف السجل - PK"]):::attribute
        AU_3(["الإجراء"]):::attribute
        AU_4(["الكيان"]):::attribute
        AU_5(["المفتاح/الرقم"]):::attribute
        AU_6(["ملاحظة"]):::attribute
        AU_7(["التاريخ"]):::attribute
    end

    E_CLIENT --- AL1
    E_CLIENT --- AL2
    AL2 --- AL2A
    AL2 --- AL2B
    E_CLIENT --- AL3
    E_CLIENT --- AL4
    E_CLIENT --- AL5
    E_CLIENT --- AL6
    AL6 --- AL6A
    AL6 --- AL6B
    E_CLIENT --- AL7

    E_USER --- AU1
    E_USER --- AU2
    E_USER --- AU3
    E_USER --- AU4
    E_USER --- AU5

    E_ROLE --- AR1
    E_ROLE --- AR2
    E_ROLE --- AR3

    E_PERM --- PM1
    E_PERM --- PM2

    E_NOTIF --- NO1
    E_NOTIF --- NO2
    E_NOTIF --- NO3
    E_NOTIF --- NO4
    E_NOTIF --- NO5
    E_NOTIF --- NO6

    E_AUDIT --- AU_1
    E_AUDIT --- AU_3
    E_AUDIT --- AU_4
    E_AUDIT --- AU_5
    E_AUDIT --- AU_6
    E_AUDIT --- AU_7
```

---

## 3) لوحة الخصائص: المنتجات التأمينية ودورة الإصدار

```mermaid
flowchart LR
    classDef entity fill:#2E86C1,stroke:#1B4F72,stroke-width:2px,color:#fff,font-weight:bold
    classDef attribute fill:#F8F9F9,stroke:#AAB7B8,stroke-width:1.5px,color:#222
    classDef derived_attribute fill:#F8F9F9,stroke:#7F8C8D,stroke-width:2px,color:#222,stroke-dasharray: 6 6

    subgraph P1["مجال التأمين"]
        direction TB
        E_CAT["مجال التأمين"]:::entity
        AC1(["معرف المجال - PK"]):::attribute
        AC2(["الاسم"]):::attribute
        AC3(["الوصف"]):::attribute
    end

    subgraph P2["نوع التأمين"]
        direction TB
        E_TYPE["نوع التأمين"]:::entity
        AT1(["معرف النوع - PK"]):::attribute
        AT2(["الاسم"]):::attribute
    end

    subgraph P2A["التغطية التأمينية"]
        direction TB
        E_COVER["التغطية التأمينية"]:::entity
        CV1(["معرف التغطية - PK"]):::attribute
        CV2(["اسم التغطية"]):::attribute
        CV3(["حد التغطية"]):::attribute
        CV4(["نسبة التحمل"]):::attribute
        CV5(["الاستثناءات"]):::attribute
    end

    subgraph P2B["قاعدة التسعير"]
        direction TB
        E_PRICE["قاعدة التسعير"]:::entity
        PR1(["معرف القاعدة - PK"]):::attribute
        PR2(["عامل التسعير"]):::attribute
        PR3(["طريقة الاحتساب"]):::attribute
        PR4(["النسبة أو المعامل"]):::attribute
        PR5(["الحالة"]):::attribute
    end

    subgraph P2C["متطلب مستند"]
        direction TB
        E_DOCREQ["متطلب مستند"]:::entity
        DR1(["معرف المتطلب - PK"]):::attribute
        DR2(["نوع المستند"]):::attribute
        DR3(["الوصف"]):::attribute
        DR4(["إلزامي"]):::attribute
    end

    subgraph P3["طلب التأمين ومرفقاته"]
        direction TB
        E_REQ["طلب التأمين"]:::entity
        AQ1(["رقم الطلب - PK"]):::attribute
        AQ2(["التاريخ"]):::attribute
        AQ3(["الحالة"]):::attribute
        AQ4(["التغطية المقترحة"]):::attribute
        AQ5(["القسط المقترح"]):::attribute
        AQ6(["نتيجة الاكتتاب"]):::attribute
        AQ7(["سبب الرفض"]):::attribute
        AQ8(["بيانات محل التأمين"]):::attribute

        E_RDOC["مرفق الطلب"]:::entity
        RD1(["معرف المرفق (Partial Key)"]):::attribute
        RD2(["نوع المرفق"]):::attribute
        RD3(["المسار"]):::attribute
        RD4(["تاريخ الرفع"]):::attribute
    end

    subgraph P4["وثيقة التأمين"]
        direction TB
        E_POL["وثيقة التأمين"]:::entity
        AP1(["معرف الوثيقة - PK"]):::attribute
        AP2(["رقم الوثيقة"]):::attribute
        AP3(["تاريخ الإصدار"]):::attribute
        AP4(["تاريخ البدء"]):::attribute
        AP5(["تاريخ الانتهاء"]):::attribute
        AP_DERIVED(["مدة الوثيقة"]):::derived_attribute
        AP7(["القسط"]):::attribute
        AP8(["الحالة"]):::attribute
        AP9(["بيانات محل التأمين"]):::attribute
        AP10(["نسبة التحمل"]):::attribute
        AP11(["الاستثناءات"]):::attribute
    end

    subgraph P6["مرفق وملحق الوثيقة"]
        direction TB
        E_PDOC["مرفق الوثيقة"]:::entity
        PD1(["معرف المرفق - PK"]):::attribute
        PD2(["نوع المستند"]):::attribute
        PD3(["اسم الملف"]):::attribute
        PD4(["المسار"]):::attribute
        PD5(["تاريخ الرفع"]):::attribute

        E_ENDORS["ملحق الوثيقة"]:::entity
        EN1(["معرف الملحق (Partial Key)"]):::attribute
        EN2(["نوع التعديل"]):::attribute
        EN3(["الوصف"]):::attribute
        EN4(["تأثير القسط"]):::attribute
        EN5(["تاريخ الإصدار"]):::attribute
    end

    E_CAT --- AC1
    E_CAT --- AC2
    E_CAT --- AC3

    E_TYPE --- AT1
    E_TYPE --- AT2

    E_COVER --- CV1
    E_COVER --- CV2
    E_COVER --- CV3
    E_COVER --- CV4
    E_COVER --- CV5

    E_PRICE --- PR1
    E_PRICE --- PR2
    E_PRICE --- PR3
    E_PRICE --- PR4
    E_PRICE --- PR5

    E_DOCREQ --- DR1
    E_DOCREQ --- DR2
    E_DOCREQ --- DR3
    E_DOCREQ --- DR4

    E_REQ --- AQ1
    E_REQ --- AQ2
    E_REQ --- AQ3
    E_REQ --- AQ4
    E_REQ --- AQ5
    E_REQ --- AQ6
    E_REQ --- AQ7
    E_REQ --- AQ8

    E_RDOC --- RD1
    E_RDOC --- RD2
    E_RDOC --- RD3
    E_RDOC --- RD4

    E_POL --- AP1
    E_POL --- AP2
    E_POL --- AP3
    E_POL --- AP4
    E_POL --- AP5
    E_POL -.- AP_DERIVED
    E_POL --- AP7
    E_POL --- AP8
    E_POL --- AP9
    E_POL --- AP10
    E_POL --- AP11

    E_PDOC --- PD1
    E_PDOC --- PD2
    E_PDOC --- PD3
    E_PDOC --- PD4
    E_PDOC --- PD5

    E_ENDORS --- EN1
    E_ENDORS --- EN2
    E_ENDORS --- EN3
    E_ENDORS --- EN4
    E_ENDORS --- EN5
```

---

## 4) لوحة الخصائص: المطالبات والسداد والتتبع

```mermaid
flowchart LR
    classDef entity fill:#2E86C1,stroke:#1B4F72,stroke-width:2px,color:#fff,font-weight:bold
    classDef weak_entity fill:#C0392B,stroke:#7B241C,stroke-width:4px,color:#fff,font-weight:bold
    classDef attribute fill:#F8F9F9,stroke:#AAB7B8,stroke-width:1.5px,color:#222

    subgraph F1["المطالبة"]
        direction TB
        E_CLAIM["المطالبة"]:::entity
        CL1(["معرف المطالبة - PK"]):::attribute
        CL2(["رقم المطالبة"]):::attribute
        CL3(["تاريخ الحادث"]):::attribute
        CL4(["مكان الحادث"]):::attribute
        CL5(["الوصف"]):::attribute
        CL6(["الخسارة المقدرة"]):::attribute
        CL7(["الحالة"]):::attribute
        CL8(["مبلغ التعويض المعتمد"]):::attribute
        CL9(["سبب الرفض"]):::attribute
    end

    subgraph F2["مرفق المطالبة"]
        direction TB
        E_CDOC["مرفق المطالبة"]:::weak_entity
        DO1(["معرف المرفق (Partial Key)"]):::attribute
        DO2(["النوع"]):::attribute
        DO3(["الاسم"]):::attribute
        DO4(["المسار"]):::attribute
        DO5(["تاريخ الرفع"]):::attribute
    end

    subgraph F3["سجل حالة المطالبة"]
        direction TB
        E_CSTAT["سجل حالة المطالبة"]:::weak_entity
        CS1(["معرف السجل (Partial Key)"]):::attribute
        CS2(["الحالة السابقة"]):::attribute
        CS3(["الحالة الجديدة"]):::attribute
        CS4(["ملاحظة"]):::attribute
        CS5(["وقت التعديل"]):::attribute
    end

    subgraph F3B["اعتراض المطالبة"]
        direction TB
        E_OBJ["اعتراض المطالبة"]:::weak_entity
        OB1(["معرف الاعتراض (Partial Key)"]):::attribute
        OB2(["سبب الاعتراض"]):::attribute
        OB3(["نص الاعتراض"]):::attribute
        OB4(["الحالة"]):::attribute
        OB5(["نتيجة المعالجة"]):::attribute
        OB6(["تاريخ التقديم"]):::attribute
    end

    subgraph F4["القسط المستحق"]
        direction TB
        E_INST["القسط المستحق"]:::weak_entity
        IN1(["معرف القسط (Partial Key)"]):::attribute
        IN2(["رقم القسط"]):::attribute
        IN3(["الاستحقاق"]):::attribute
        IN4(["المبلغ"]):::attribute
        IN5(["الحالة"]):::attribute
    end

    subgraph F5["عملية السداد"]
        direction TB
        E_PAY["عملية السداد"]:::entity
        PY1(["معرف الدفعة - PK"]):::attribute
        PY2(["المبلغ المسدد"]):::attribute
        PY3(["طريقة الدفع"]):::attribute
        PY4(["المرجع"]):::attribute
        PY5(["التاريخ"]):::attribute
    end

    E_CLAIM --- CL1
    E_CLAIM --- CL2
    E_CLAIM --- CL3
    E_CLAIM --- CL4
    E_CLAIM --- CL5
    E_CLAIM --- CL6
    E_CLAIM --- CL7
    E_CLAIM --- CL8
    E_CLAIM --- CL9

    E_CDOC --- DO1
    E_CDOC --- DO2
    E_CDOC --- DO3
    E_CDOC --- DO4
    E_CDOC --- DO5

    E_CSTAT --- CS1
    E_CSTAT --- CS2
    E_CSTAT --- CS3
    E_CSTAT --- CS4
    E_CSTAT --- CS5

    E_OBJ --- OB1
    E_OBJ --- OB2
    E_OBJ --- OB3
    E_OBJ --- OB4
    E_OBJ --- OB5
    E_OBJ --- OB6

    E_INST --- IN1
    E_INST --- IN2
    E_INST --- IN3
    E_INST --- IN4
    E_INST --- IN5

    E_PAY --- PY1
    E_PAY --- PY2
    E_PAY --- PY3
    E_PAY --- PY4
    E_PAY --- PY5
```

---

## 5) ملاحظات تنسيق مناسبة لملف Word

- عند إدراج المخطط في Word، استخدم كل لوحة كصورة مستقلة بدلاً من لقطة واحدة شديدة الاتساع.
- الأفضل وضع **اللوحة الرئيسية** أولاً، ثم اللوحات التفصيلية أسفلها كتفصيل مرجعي.
- هذا يمنحك شكلاً احترافياً قريباً من الرسومات الأكاديمية المنشورة، لكنه أوضح لأن العلاقات العامة منفصلة عن ازدحام الخصائص.
